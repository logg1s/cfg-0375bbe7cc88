## 0.4.9 - 2026-06-01

### Changed

- Stop hook now requires final memory disposition for non-trivial work: record durable `decision`, `fact`, `verify`, or `finding` entries with ids, or explicitly state that no durable memory was written.
- Memory skill docs now map durable project behavior to the correct memory type and include decision/verification examples.
- Installed Agent Kit rules now keep the memory-disposition requirement compact while preserving the lean baseline-first prompt.
